# Client environment — copy to client/.env and fill in.
# These are baked in at BUILD time (Vite), not read at runtime.

# Clerk publishable key (pk_test_... for dev)
VITE_CLERK_PUBLISHABLE_KEY=

# Backend base URL, no trailing slash. Leave blank for local dev (proxied to :3001).
VITE_API_URL=
